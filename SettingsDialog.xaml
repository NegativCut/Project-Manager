<Window x:Class="ProjectManager.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="320" Width="550"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FF1E1E1E">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF0E639C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF2D2D30"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
            <Setter Property="BorderBrush" Value="#FF3F3F46"/>
            <Setter Property="CaretBrush" Value="#FFE0E0E0"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Engineering Root Directory" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Name="txtEngineeringRoot" Grid.Column="0" Height="25"
                         VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Name="btnBrowse" Grid.Column="1" Content="Browse..." Width="80" Height="25" Click="BtnBrowse_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Datasheets Directory" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Name="txtDatasheetsPath" Grid.Column="0" Height="25"
                         VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Name="btnBrowseDatasheets" Grid.Column="1" Content="Browse..." Width="80" Height="25" Click="BtnBrowseDatasheets_Click"/>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Information" Margin="0,0,0,10">
            <StackPanel Margin="10">
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                    <Run FontWeight="Bold">Engineering Root:</Run> Where project folders are created.
                </TextBlock>
                <TextBlock TextWrapping="Wrap" Foreground="#FF9D9D9D" Margin="0,0,0,10">
                    Example: D:\Engineering\Altium\0001_PowerSupply_Rev1\
                </TextBlock>
                <TextBlock TextWrapping="Wrap">
                    <Run FontWeight="Bold">Datasheets:</Run> Your datasheet repository (can be any location).
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnSave" Content="Save" Width="80" Height="30" Margin="5" Click="BtnSave_Click"/>
            <Button Name="btnCancel" Content="Cancel" Width="80" Height="30" Margin="5" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
